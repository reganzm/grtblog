import { PageContainer } from '@ant-design/pro-components';

const PluginList = () => {
  return (
    <PageContainer title={'插件列表'}>
      <div> 当前版本为了稳定考虑，暂不支持插件功能，将在后续版本中支持</div>
      <div> 插件开发在 dev 分支，将在准备好之后合并到主分支</div>
    </PageContainer>
  );
};

export default PluginList;
